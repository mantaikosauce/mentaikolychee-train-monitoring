# Runtime dependencies for the app and all subsystem packages.
# Install into a fresh venv:  python -m venv .venv && .venv/Scripts/python -m pip install -r requirements.txt
numpy
pandas
scipy
scikit-learn==1.9.1   # the bundled Rail/ACV artifacts were pickled with 1.9.1; predictions verified identical on 1.8.0
joblib
openpyxl
rainflow
streamlit>=1.46
plotly
pydeck
requests
pytest
