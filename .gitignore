# The cloned competition dataset: 7.6 GB. Never commit it.
# Restore with the clone command in HANDOFF.md.
repo/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/
*.egg-info/

# Environments — rebuilt from environment.yml, never committed
.venv/
venv/
env/

# OS
Thumbs.db
desktop.ini
.DS_Store

# Scratch
scratch/
*.tmp

# Model artifacts ARE committed on purpose: they are small, and the handoff
# contract says a teammate must be able to run the smoke test without
# retraining. If an artifact ever exceeds ~50 MB, revisit this.
artifacts_cache/

# derived dashboard cache, rebuilt by scripts/build_predictions.py
predictions/analysis_cache.pkl
data/events.jsonl
data/events_actions.jsonl
app/static/
data/uploads/
data/retrain_log.json

# Secrets - the LTA DataMall AccountKey lives here. Never commit.
.streamlit/secrets.toml
.env
*.env
